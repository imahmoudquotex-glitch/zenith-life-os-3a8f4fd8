# Performance Budgets — Wave 04 Baseline

**Date:** 2026-05-17  
**Build:** `pnpm build` — completed in 4.36s

## Targets

| Metric | Target | Status |
|---|---|---|
| LCP | < 2.5s | 🟡 To be measured |
| CLS | < 0.1 | 🟡 To be measured |
| INP | < 200ms | 🟡 To be measured |
| First JS bundle | < 200KB gzip | 🟡 To be measured |

## Build Output (Wave 04)

```
dist/ — generated by vite build
✓ Built in 4.36s (as of 2026-05-17)
```

Bundle analysis to be added in W05 after Lighthouse CI is configured.

## Font Strategy

- Cairo: `font-display: swap` + `<link rel="preload">`
- Inter: `font-display: swap` + `<link rel="preload">`
- JetBrains Mono: loaded on demand

## Image Strategy

All images use `<img>` with explicit `width`/`height` to prevent CLS.
Next.js `<Image>` migration deferred — this is a Vite/TanStack Router app.

## Measurements (TODO W05)

Run after W05 deployment:
```bash
npx lighthouse https://your-app-url --output json --chrome-flags="--headless"
```

Record results in this file.
