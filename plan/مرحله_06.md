# ðŸ›¡ï¸ HYPER-UPGRADE PACK â€” Master Silent Refinement v2026.05.15

> contract Ø¥Ø¬Ø¨Ø§Ø±ÙŠ. Ù…Ø±Ø¬Ø¹ Phase 0.
> 

## A. Invariants

Desktop-PWA + Dark + Multi-tenant + ZKE + AI Safety + WCAG 2.2 AA + Donations-only.

## B. Phase-Specific Deep Additions â€” Wave 05 (Dashboard + Settings)

### B.1 No-AI-in-Render Enforcement

- ESLint custom rule `no-ai-in-render-path` ÙŠÙØ­Øµ Ø£ÙŠ Ù…Ù„Ù Ø¨Ù€ `'use client'` Ø£Ùˆ RSC Ø£Ùˆ page.tsx ÙˆÙŠØ±ÙØ¶ Ø£ÙŠ import Ù…Ø¨Ø§Ø´Ø±/transitive Ù„Ù€ `runAIWithQuota`.
- Runtime guard: `assertNoAIInRender()` ÙŠØ³ØªØ®Ø¯Ù… AsyncLocalStorage Ù„ØªØªØ¨Ø¹ call stack Ùˆthrows ÙÙŠ dev mode.
- AI suggestions = explicit user action ÙÙ‚Ø· (click button) Ø¹Ø¨Ø± server action.

### B.2 Widget Resilience Pattern

- Each widget = Suspense + ErrorBoundary + Skeleton + EmptyState + safeWidget(name, fn, fallback).
- Logger: pino structured `{ widget, userId, workspaceId, requestId, latencyMs, err? }`.
- Telemetry: `dashboard_widget_render_total{widget,status}` Prometheus.
- SLO: widget failure rate < 1%, p95 < 200ms.

### B.3 Settings Privacy & Vault Lock-Down

- DB trigger `user_settings_force_vault_off` ÙŠÙØ±Ø¶ `allowVaultContext = false` Ø¹Ù„Ù‰ INSERT/UPDATE.
- Server action ÙŠØ±ÙØ¶ Ø£ÙŠ patch ÙÙŠÙ‡ `allowVaultContext: true` Ø¨Ù€ 403.
- ESLint rule ÙŠÙ…Ù†Ø¹ `localStorage.setItem` Ù„Ø£ÙŠ vault-related key.
- Audit event on each settings change Ù…Ø¹ `before_hash`/`after_hash` redacted.

### B.4 Dashboard Read Model

- `dashboard_snapshot_view` materialized view per (workspace_id, user_id, date) refreshed ÙƒÙ„ 5min.
- Cache 30s per user Ø¹Ø¨Ø± `unstable_cache` + revalidate-tag `dashboard:{workspaceId}:{userId}`.
- Invalidation triggered by: task complete, habit checkin, expense add, mood log, settings change.
- N+1 protection: single aggregator service Ù…Ø¹ `Promise.all` + safeWidget.

### B.5 Notes Privacy (Titles-Only)

- Dashboard widget ÙŠØ±Ø¬Ø¹ `recentNoteTitlesOnly({ limit: 5 })` â€” `SELECT id, title, updated_at` ÙÙ‚Ø·.
- Vault notes excluded Ø¹Ø¨Ø± `WHERE NOT is_vault AND NOT is_deleted`.
- CI test ÙŠØ¶Ù…Ù† Ø£Ù† Ø§Ù„Ù†Øµ Ø§Ù„ÙƒØ§Ù…Ù„ Ù„Ø§ ÙŠØ¸Ù‡Ø± ÙÙŠ dashboard response.

### B.6 Anti-Patterns Additions

- âŒ AI call ÙÙŠ render path (CI scan).
- âŒ `console.log` (use logger).
- âŒ `as any` casting.
- âŒ Direct Supabase client Ù…Ù† UI (use service).
- âŒ Settings update ÙŠØ³ØªØ¨Ø¯Ù„ JSON ÙƒØ§Ù…Ù„Ø§Ù‹ (use deep merge).
- âŒ Widget Ø¨Ø¯ÙˆÙ† safeWidget wrapper.
- âŒ References Ù„Ù€ deprecated `public.profiles` (use `user_settings`).

---

# ðŸ  Wave 05 â€” Zenith Dashboard Wiring + Settings

## ðŸ§  ØªØ·ÙˆÙŠØ± ØªÙ†ÙÙŠØ°ÙŠ Ø¥Ø¶Ø§ÙÙŠ â€” Dashboard Aggregation Code Pack

Dashboard Ù„Ø§Ø²Ù… ÙŠØ¨Ù‚Ù‰ command center Ø³Ø±ÙŠØ¹ØŒ Ù…Ø´ ØµÙØ­Ø© Ø¨ØªØ¬Ù…Ø¹ queries Ø¹Ø´ÙˆØ§Ø¦ÙŠÙ‹Ø§.

### Aggregator Service

```tsx
// src/features/dashboard/server/get-dashboard-snapshot.ts
import 'server-only'
import { unstable_cache } from 'next/cache'

export const getDashboardSnapshot = (workspaceId: string, userId: string) =>
  unstable_cache(
    async () => {
      const [tasks, habits, finance, notes, lifeScore, energy] = await Promise.all([
        safeWidget('tasks', () => taskService.todaySummary({ workspaceId, userId }), { open: 0, done: 0 }),
        safeWidget('habits', () => habitService.todaySummary({ workspaceId, userId }), []),
        safeWidget('finance', () => financeService.monthAggregate({ workspaceId, userId }), { spentCents: 0 }),
        safeWidget('notes', () => noteService.recentTitlesOnly({ workspaceId, userId, limit: 5 }), []),
        safeWidget('lifeScore', () => reviewService.lifeScore({ workspaceId, userId }), { score: 0, delta: 0 }),
        safeWidget('energy', () => energyService.curve({ workspaceId, userId }), []),
      ])
      return { tasks, habits, finance, notes, lifeScore, energy }
    },
    [`dashboard:${workspaceId}:${userId}`],
    { revalidate: 30, tags: [`dashboard:${workspaceId}:${userId}`] },
  )()
```

### Safe Widget With Logger

```tsx
export async function safeWidget<T>(name: string, fn: () => Promise<T>, fallback: T): Promise<T> {
  try {
    return await fn()
  } catch (err) {
    logger.warn({ err, widget: name }, 'dashboard_widget_failed')
    return fallback
  }
}
```

### Settings Audit Guard

```tsx
export async function updatePrivacySettings(input: PrivacySettingsPatch, ctx: RequestContext) {
  if ('allowVaultContext' in JSON.stringify(input)) {
    throw new AppError('VAULT_CONTEXT_FORBIDDEN', 'Vault context cannot be enabled', 403)
  }
  const before = await settingsRepo.get(ctx.workspaceId, ctx.userId)
  const after = deepMerge(before, sanitizeSettingsPatch(input))
  await settingsRepo.update(ctx.workspaceId, ctx.userId, after)
  await audit.write({ action: 'settings.privacy.updated', before: redact(before), after: redact(after), actorId: ctx.userId })
}
```

## ðŸ§  Master Architect Upgrade â€” Dashboard as Desktop Command Center

- **Dashboard read model:** Ø§Ø¹Ù…Ù„ `dashboard_today_snapshot` Ø£Ùˆ aggregator service ÙˆØ§Ø­Ø¯ server-only ÙŠØ¬Ù…Ø¹ widgets Ø¨Ù€ cache tagsØŒ Ø¨Ø¯ÙˆÙ† AIØŒ ÙˆØ¨Ø¯ÙˆÙ† N+1 queries.
- **Widget isolation:** ÙƒÙ„ widget = Suspense + skeleton + empty + error + `safeWidget` + telemetry. ÙØ´Ù„ widget Ù„Ø§ ÙŠÙƒØ³Ø± Ø§Ù„ØµÙØ­Ø© ÙˆÙ„Ø§ ÙŠØ­Ø±Ù‚ quota.
- **Privacy slices:** Notes ÙÙŠ Dashboard Ø¹Ù†Ø§ÙˆÙŠÙ† ÙÙ‚Ø·ØŒ Vault Ù…Ù…Ù†ÙˆØ¹ØŒ Finance ÙŠØ¹Ø±Ø¶ aggregates ÙÙ‚Ø·ØŒ AI suggestions on-click ÙÙ‚Ø·.
- **Desktop interactions:** shortcuts Ø¥Ù„Ø²Ø§Ù…ÙŠØ©: `Cmd/Ctrl+K` command paletteØŒ `Q` quick captureØŒ `F` focusØŒ `/` Ø¯Ø§Ø®Ù„ note editor ÙÙ‚Ø·ØŒ Ùˆ`?` keyboard help.
- **Settings as policy surface:** Ø£ÙŠ setting ÙŠØ¤Ø«Ø± Ø¹Ù„Ù‰ Ø§Ù„Ø®ØµÙˆØµÙŠØ©/Ø§Ù„Ø®Ø²Ù†Ø©/AI ÙŠØ­ØªØ§Ø¬ server validation + audit + confirmation. `allowVaultContext=false` ÙŠØ¨Ù‚Ù‰ DB invariant ÙˆÙ„ÙŠØ³ UI preference.

## ðŸš€ Antigravity Direct Fix Mode â€” W05

<aside>
ðŸš€

**Dashboard fixes ØªØªÙ†ÙØ° Ø¯Ø§Ø®Ù„ Ø§Ù„Ù…Ø±Ø­Ù„Ø© Ù†ÙØ³Ù‡Ø§ØŒ Ù…Ø´ ÙÙŠ ØªÙ‚Ø±ÙŠØ± Ø®Ø§Ø±Ø¬ÙŠ.**

Antigravity ÙŠØ¨Ø¯Ù‘Ù„ snippets Ø§Ù„Ù…Ø®Ø§Ù„ÙØ© ÙÙˆØ±Ù‹Ø§: `console.error` â†’ loggerØŒ `as any` â†’ types/guardsØŒ `profiles` â†’ `users/user_settings` canonicalØŒ ÙˆAI buttons ØªØ¨Ù‚Ù‰ actions ÙÙ‚Ø· Ø¨Ø¯ÙˆÙ† render-path calls.

Ø£ÙŠ widget Ù„Ø§ ÙŠÙ…Ù„Ùƒ `safeWidget` + skeleton + empty + error state ÙŠØ¹ØªØ¨Ø± Task ØºÙŠØ± Ù…ÙƒØªÙ…Ù„.

</aside>

## ðŸ›¡ï¸ ØªØµØ­ÙŠØ­ Ù…Ø¹Ù…Ø§Ø±ÙŠ Ø¥Ù„Ø²Ø§Ù…ÙŠ â€” W05-A

<aside>
ðŸš¨

**Dashboard wiring Ù…Ù…Ù†ÙˆØ¹ ÙŠØªØ­ÙˆÙ„ Ù„ÙƒÙˆØ¯ Ù…Ø®Ø§Ù„Ù Ù„Ù€ W00 Strict Mode.**

Ø£ÙŠ snippet Ù‚Ø¯ÙŠÙ… ÙÙŠÙ‡ `console.error`, `any`, `profiles`, Ø£Ùˆ direct Supabase calls Ù…Ù† UI ÙŠØªØ¹Ø¯Ù„ Ù‚Ø¨Ù„ Ø§Ù„ØªÙ†ÙÙŠØ°.

</aside>

## ðŸ”§ V2 Canonical Fix â€” W05 (Ø¥Ù„Ø²Ø§Ù…ÙŠ)

<aside>
âœ…

**Ù‡Ø°Ø§ Ø§Ù„Ù‚Ø³Ù… ÙŠÙ„ØºÙŠ ÙˆÙŠØ³ØªØ¨Ø¯Ù„ Ø£ÙŠ snippet Ø£Ø³ÙÙ„Ù‡ ÙŠØ³ØªØ®Ø¯Ù… `public.profiles`. Ø§Ù„Ø³ÙˆØ±Ø³ Ø§Ù„ÙˆØ­ÙŠØ¯ Ù„Ù„Ø­Ù‚ÙŠÙ‚Ø© Ù‡Ùˆ Ø§Ù„Ø³ÙƒØ±ÙŠØ¨Øª Ø§Ù„Ù€ canonical Ø¨Ø§Ù„Ø£Ø³ÙÙ„.**

</aside>

**Migration 0510 â€” Settings Vault Guard (Canonical):**

```sql
-- supabase/migrations/0510_user_settings_vault_guard.sql
BEGIN;

-- Ø§Ø³ØªØ®Ø¯Ù… user_settings Ø§Ù„Ù€ canonical Ù…Ù† W01 (TEXT ULID + workspace_id) Ù…Ø´ public.profiles
CREATE OR REPLACE FUNCTION public.user_settings_force_vault_off()
RETURNS TRIGGER LANGUAGE plpgsql AS $$
BEGIN
	IF NEW.settings IS NOT NULL THEN
		NEW.settings = jsonb_set(
			COALESCE(NEW.settings, '{}'::jsonb),
			'{ai,allowVaultContext}',
			'false'::jsonb,
			true
		);
	END IF;
	RETURN NEW;
END $$;

DROP TRIGGER IF EXISTS trg_user_settings_force_vault_off ON public.user_settings;
CREATE TRIGGER trg_user_settings_force_vault_off
BEFORE INSERT OR UPDATE ON public.user_settings
FOR EACH ROW EXECUTE FUNCTION public.user_settings_force_vault_off();

-- Server action ÙŠØ¹Ø¯Ù„ Ø¹Ù„Ù‰ user_settings Ù…Ø´ profiles
COMMENT ON TRIGGER trg_user_settings_force_vault_off ON public.user_settings IS
	'V2 Canonical: replaces legacy public.profiles trigger. workspace_id-scoped via RLS.';

COMMIT;
```

**Server Action Fix:**

```tsx
// src/features/settings/actions/update-settings.ts (V2)
const { data: cur } = await serviceSupabase
	.from('user_settings') // âœ… Ù„ÙŠØ³ 'profiles'
	.select('settings, timezone')
	.eq('user_id', user.id)
	.eq('workspace_id', currentWorkspaceId) // âœ… tenant isolation
	.single()

await serviceSupabase.from('user_settings').update({
	settings: merged,
	...(parsed.timezone ? { timezone: parsed.timezone } : {}),
}).eq('user_id', user.id).eq('workspace_id', currentWorkspaceId)
```

**CI Guard:**

```bash
! grep -RE "from\\('profiles'\\)|public\\.profiles" apps packages supabase/migrations/05*
```

### Fixes Ø¥Ù„Ø²Ø§Ù…ÙŠØ©

- `safeWidget()` ÙŠØ³ØªØ®Ø¯Ù… `logger.warn({ err, widgetName, requestId }, 'dashboard.widget_failed')` Ø¨Ø¯Ù„ `console.error`.
- Ù…Ù…Ù†ÙˆØ¹ `(globalThis as any)` ÙÙŠ no-AI guard. Ø§Ø³ØªØ®Ø¯Ù… typed global declaration Ø£Ùˆ lint rule ÙÙ‚Ø·.
- Ø£ÙŠ `profiles` table reference ÙŠØªØ¹Ø¯Ù„ Ø¥Ù„Ù‰ canonical W01 `users` Ø£Ùˆ `user_settings` table Ù…ÙˆØ«Ù‚. Ù„Ø§ ØªÙ†Ø´Ø¦ `profiles` Ø¬Ø¯ÙŠØ¯.
- Settings updates ØªØªÙ… Ø¹Ø¨Ø± service layer + deep merge + audit. Ù…Ù…Ù†ÙˆØ¹ client ÙŠØ±Ø³Ù„ `allowVaultContext`; DB trigger ÙŠÙØ±Ø¶ false.
- Dashboard data ÙŠÙ‚Ø±Ø£ Ù…Ù† system DBs/services ÙÙ‚Ø·ØŒ ÙˆÙ„ÙŠØ³ Ù…Ù† mock Ø£Ùˆ table parallel.

### Antigravity command

```bash
pnpm tsx scripts/check-no-ai-in-render.ts
pnpm tsx scripts/check-dashboard-widgets.ts
pnpm tsx scripts/check-settings-vault-guard.ts
! grep -R "console\\." apps packages
! grep -R " as any\\|: any" apps packages
```

- Dashboard visuals ÙÙŠ Zenith ØªÙØ¹ØªØ¨Ø± frozen baseline: Life Score RingØŒ Energy CurveØŒ XP/LevelØŒ heroØŒ CTAsØŒ Sidebar.
- ÙƒÙ„ widget Ù„Ø§Ø²Ù… ÙŠÙƒÙˆÙ† Ù…Ø³ØªÙ‚Ù„: Suspense boundary + skeleton + empty + error state + `safeWidget()`.
- Notes ÙÙŠ Dashboard = titles onlyØŒ Ù…Ù…Ù†ÙˆØ¹ full content.
- Settings updates = deep merge ÙÙ‚Ø·ØŒ Ùˆ`allowVaultContext` ÙŠØ¸Ù„ false Ù…Ù† app + DB trigger.

### Checklist Ù…Ø±Ø§Ø¬Ø¹Ø© Ø³Ø±ÙŠØ¹ Ù‚Ø¨Ù„ `w05-frozen`

- [x]  `w04-frozen` Ù…ÙˆØ¬ÙˆØ¯.
- [x]  Dashboard mock data Ø§ØªØ¨Ø¯Ù„Øª Ø¨Ù€ Supabase queries.
- [x]  cache 30s Ù„ÙƒÙ„ user + invalidation tags.
- [x]  no AI in render path Ù…Ø«Ø¨Øª Ø¨Ø§Ø®ØªØ¨Ø§Ø±.
- [x]  Sidebar routes ØªØ¹Ù…Ù„ ÙˆØªÙØªØ­ placeholders Ø¨Ø¯ÙˆÙ† ÙƒØ³Ø± Ø§Ù„ØªØµÙ…ÙŠÙ….

<aside>
ðŸš¨

**[Ø§Ù„ØªØ­Ø¯ÙŠØ« 2026-05-13] â€” Dashboard Ù…ÙˆØ¬ÙˆØ¯ ÙØ¹Ù„Ø§Ù‹ ÙÙŠ Zenith. Ù…Ù…Ù†ÙˆØ¹ Ø¨Ù†Ø§Ø¡ Ø¬Ø¯ÙŠØ¯.**

**Repo:** https://github.com/imahmoudquotex-glitch/zenith-life-os-3a8f4fd8

Ø§Ù„Ù€ Dashboard UI ÙƒØ§Ù…Ù„Ù‹Ø§ (Life Score Ring 83ØŒ Energy CurveØŒ XP 6,500 Lv 7ØŒ Focus Session 47 Ø¯ØŒ AI day planner) Ù…ÙˆØ¬ÙˆØ¯ ÙÙŠ Ø§Ù„Ù€ repo. Ø¯ÙˆØ± Antigravity Ù‡Ù†Ø§: **Ø§Ø±Ø¨Ø· Ø§Ù„Ù€ widgets Ø¨Ù€ Supabase + safeWidget pattern + cache + Ù„Ø§ AI ÙÙŠ render path + Settings pages**.

</aside>

## âš¡ Zenith Dashboard Directive

### Ù…Ø§ Ù‡Ùˆ Ù…ÙˆØ¬ÙˆØ¯ ÙØ¹Ù„Ø§Ù‹ ÙÙŠ Zenith repo (Dashboard / Ø§Ù„Ø±Ø¦ÙŠØ³ÙŠØ©):

- âœ… **Life Score Ring** Ø¨Ù‚ÙŠÙ…Ø© 83 â€” progress ring Ø£Ø®Ø¶Ø± Ù†ÙŠÙˆÙ† Ù…Ø¹ indicator 'â†‘ 12.5 Ù‡Ø°Ø§ Ø§Ù„Ø£Ø³Ø¨ÙˆØ¹'.
- âœ… **Energy Curve chart** â€” bar chart Ù„Ù„Ù€ 24 Ø³Ø§Ø¹Ø© Ù…Ø¹ time block labels: 09:00 Ù…Ø±Ø§Ø¬Ø¹Ø© Ø§Ù„Ø®Ø·Ø© Â· 11:00 ØªÙ†ÙÙŠØ° Ø¹Ù…ÙŠÙ‚ Â· 14:00 Ù…ÙŠØªÙ†Øº Ø§Ù„ÙØ±ÙŠÙ‚ Â· 17:00 Ù…Ø±Ø§Ø¬Ø¹Ø© Ù…Ø§Ù„ÙŠØ©.
- âœ… **XP/Level Display** (XP 6,500 / Lv 7) + Ø¥Ø´Ø¹Ø§Ø± 'Ø°Ø±ÙˆØ© Ø·Ø§Ù‚ØªÙƒ Ø®Ù„Ø§Ù„ 47 Ø¯Ù‚ÙŠÙ‚Ø©'.
- âœ… **Hero text** 'Ø£Ù†Øª ÙÙŠ Ø£ÙØ¶Ù„ Ø£ÙŠØ§Ù…Ùƒ' + subtitle 'Ø¥Ù†ØªØ§Ø¬ÙŠØªÙƒ Ø§Ù„ÙŠÙˆÙ… ØªØªØ¬Ø§ÙˆØ² Ù…Ø¹Ø¯Ù„Ùƒ Ø§Ù„Ø£Ø³Ø¨ÙˆØ¹ÙŠ Ø¨Ù€ 12%. Ù„Ø¯ÙŠÙƒ Ù†Ø§ÙØ°Ø© ØªØ±ÙƒÙŠØ² Ù…Ù…ØªØ§Ø²Ø© Ø­ØªÙ‰ Ø§Ù„Ø³Ø§Ø¹Ø© 2 Ø¸Ù‡Ø±Ù‹Ø§ â€” Ø§Ø³ØªØ«Ù…Ø±Ù‡Ø§ ÙÙŠ Ø£ØµØ¹Ø¨ 3 Ù…Ù‡Ø§Ù….'
- âœ… **Daily stats row** â€” 5/9 Ù…Ù‡Ø§Ù… Â· ÙŠÙˆÙ… 31 Â· 64% Ù…ÙŠØ²Ø§Ù†ÙŠØ© Â· 2 Ø±Ø³Ø§Ø¦Ù„ AI.
- âœ… **CTA Buttons**: 'Ø§Ø¨Ø¯Ø£ Ø¬Ù„Ø³Ø© ØªØ±ÙƒÙŠØ² 47 Ø¯ â–¶' + 'Ø±ØªØ¨ ÙŠÙˆÙ…ÙŠ Ø¨Ø§Ù„Ø°ÙƒØ§Ø¡ âœ¨'.
- âœ… **Mission Control** indicator top-right.
- âœ… **Sidebar**: Ø§Ù„Ø±Ø¦ÙŠØ³ÙŠØ© (active) Â· Ø§Ù„Ù…Ø´Ø§Ø±ÙŠØ¹ Â· Ø§Ù„Ø¹Ø§Ø¯Ø§Øª Â· Ø§Ù„Ù…Ù„Ø§Ø­Ø¸Ø§Øª Â· Ø§Ù„ØªÙ‚ÙˆÙŠÙ… Â· Ø§Ù„Ø£Ù‡Ø¯Ø§Ù Â· Ø§Ù„Ø®Ø²Ù†Ø© Â· Ø§Ù„Ù…ØµØ§Ø±ÙŠÙ Â· Ø§Ù„ØªØ­Ù„ÙŠÙ„Ø§Øª Â· Ø§Ù„Ø¥Ø¹Ø¯Ø§Ø¯Ø§Øª + User pill (Ø£Ø­Ù…Ø¯ Ù…Ø­Ù…Ø¯ / Ø§Ù„Ù…Ø¤Ø³Ø³) + Life Score mini (83).

### Ø¯ÙˆØ± Antigravity ÙÙŠ Ø§Ù„Ù€ Wave Ø¯ÙŠ:

1. **Wire Ø§Ù„Ù€ Dashboard widgets Ø¨Ù€ Supabase** (Ø§Ø³ØªØ¨Ø¯Ù„ mock data):
    - `getTodayTasks(userId)` â†’ Tasks table.
    - `getTodayHabits(userId)` â†’ Habits table.
    - `getMonthExpenseSummary(userId)` â†’ Expenses aggregate.
    - `getRecentNoteTitlesOnly(userId, { limit: 5 })` â€” **Ø¹Ù†Ø§ÙˆÙŠÙ† ÙÙ‚Ø·ØŒ Ù„Ø§ Ù…Ø­ØªÙˆÙ‰**.
    - `getEnergyCurve(userId, date)` â†’ Energy points table (24 bars).
    - `getLifeScore(userId)` â†’ computed aggregate + delta.
    - `getXpAndLevel(userId)` â†’ gamification table.
    - `getNextFocusWindow(userId)` â†’ energy curve peak detection.
2. **safeWidget Pattern** (Ù‚Ø³Ù… Step 06.1 ØªØ­Øª):
    - ÙƒÙ„ widget ÙŠØ³ØªØ¯Ø¹ÙŠ `safeWidget()` Ø¨Ù€ fallback Ø¢Ù…Ù†.
    - widget ÙØ§Ø´Ù„ = fallbackØŒ Ù…Ø´ crash Ø§Ù„Ù€ dashboard ÙƒÙ„Ù‡.
3. **Cache 30s per user** Ø¨Ù€ `unstable_cache` (Ù‚Ø³Ù… Step 06.2) + tag-based invalidation.
4. **No AI in render path** (Ù‚Ø³Ù… Step 06.5):
    - assertion ÙÙŠ dev mode.
    - lint rule ÙÙŠ CI.
    - AI buttons ('Ø±ØªØ¨ ÙŠÙˆÙ…ÙŠ Ø¨Ø§Ù„Ø°ÙƒØ§Ø¡') = action triggers Ø¨Ø³ØŒ Ù…Ø´ auto-run.
5. **Skeleton + Empty + Error states** Ù„ÙƒÙ„ widget Ù…Ø³ØªÙ‚Ù„ (Ù‚Ø³Ù… Step 06.4).
6. **Settings Pages** â€” Ø£Ø¶ÙÙ‡Ù… Ø¨Ù†ÙØ³ design language Ø§Ù„Ù…ÙˆØ¬ÙˆØ¯ ÙÙŠ Zenith:
    - Profile Â· Notifications Â· Privacy Â· Danger Zone.
    - Deep merge Ù„Ù„Ù€ settings (Ù„Ø§ replace).
    - DB trigger ÙŠÙØ±Ø¶ `allowVaultContext = false`.
    - Notification rate limit 5/hour/type.
    - IANA timezone validation.
7. **Sidebar Wiring**: Ø±ÙˆØ§Ø¨Ø· Sidebar Ø§Ù„Ù€ 9 Ù„Ø§Ø²Ù… ØªØ´ØªØºÙ„ ÙˆØªØ±ÙˆØ­ Ù„Ù„ØµÙØ­Ø§Øª Ø§Ù„Ù€ placeholder. Ø§Ù„ØµÙØ­Ø§Øª Ù†ÙØ³Ù‡Ø§ Ù‡ØªØªØ±ÙØ¹ ÙÙŠ Waves 09-16ØŒ Ù„ÙƒÙ† Ø§Ù„Ù€ routing Ù„Ø§Ø²Ù… ÙŠØ´ØªØºÙ„ Ø¯Ù„ÙˆÙ‚ØªÙŠ.

### Required Inputs Handshake â€” Ø±Ø³Ø§Ù„Ø© Ø¥Ù„Ø²Ø§Ù…ÙŠØ© Ù‚Ø¨Ù„ Ø§Ù„Ø¨Ø¯Ø¡:

```markdown
Ø£Ù†Ø§ Antigravity ÙˆØ¬Ø§Ù‡Ø² Ø£Ø¨Ø¯Ø£ Wave 05 (Dashboard wiring + Settings). Ù…Ø­ØªØ§Ø¬ ØªØ£ÙƒÙŠØ¯:

### Ø¥Ø¬Ø¨Ø§Ø±ÙŠ:
1. âœ… Wave 04 frozen (`w04-frozen` tag Ù…ÙˆØ¬ÙˆØ¯)ØŸ
2. âœ… Zenith repo cloned ÙˆÙŠØ´ØªØºÙ„ (Dashboard ÙŠÙØªØ­ Ø¹Ù„Ù‰ localhost:3000 ÙˆÙŠØ¹Ø±Ø¶ Life Score 83)ØŸ
3. âœ… Supabase tables Ù…Ù† W02 Ù…ÙˆØ¬ÙˆØ¯Ø© (tasks, habits, expenses, notes, energy_points, life_score)ØŸ
4. âœ… RLS Ù…Ù† W03 Ø´ØºÙ‘Ø§Ù„Ø© Ø¹Ù„Ù‰ ÙƒÙ„ Ø§Ù„Ø¬Ø¯Ø§ÙˆÙ„ØŸ
5. âœ… Auth Ù…Ù† W04 Ø´ØºÙ‘Ø§Ù„ (signin / signup ÙŠØ¹Ù…Ù„ÙˆØ§)ØŸ
6. âœ… Migrations 0500â€“0508 Ø¬Ø§Ù‡Ø²ÙŠÙ† Ù„Ù„ØªÙ†ÙÙŠØ°ØŸ

### Ù‚ÙˆØ§Ø¹Ø¯:
- Ù…Ù…Ù†ÙˆØ¹ ØªØºÙŠÙŠØ± Ø£ÙŠ visual ÙÙŠ Dashchat (Life Score Ring, Energy Curve, XP Bar).
- Ù…Ù…Ù†ÙˆØ¹ AI call ÙÙŠ render path.
- notes = Ø¹Ù†Ø§ÙˆÙŠÙ† ÙÙ‚Ø·ØŒ Ù„Ø§ content (privacy).
- ÙƒÙ„ widget ÙÙŠ Suspense boundary Ù…Ø³ØªÙ‚Ù„.
```

### Anti-pattern Auto-Reject:

- âŒ ØªØºÙŠÙŠØ± Dashboard layout Ø£Ùˆ Ø£ÙŠ visual element.
- âŒ AI call ÙÙŠ `getTodayBundle()` Ø£Ùˆ widget render.
- âŒ Ø¹Ø±Ø¶ Ù…Ø­ØªÙˆÙ‰ notes ÙƒØ§Ù…Ù„ (Ø¹Ù†Ø§ÙˆÙŠÙ† ÙÙ‚Ø·).
- âŒ widget Ø¨Ø¯ÙˆÙ† `safeWidget()` wrapper.
- âŒ Settings update ÙŠØ³ØªØ¨Ø¯Ù„ JSON ÙƒØ§Ù…Ù„Ù‹Ø§ (use deep merge).
- âŒ `allowVaultContext = true` from client (DB trigger ÙŠÙ…Ù†Ø¹Ù‡ + app code ÙŠÙØ±Ø¶ false).
- âŒ Notification Ø¨Ø¯ÙˆÙ† rate limit.
- âŒ Timezone Ø¨Ø¯ÙˆÙ† IANA validation.
- âŒ Suspense ÙˆØ§Ø­Ø¯ ÙŠÙ„Ù Ø§Ù„Ù€ dashboard ÙƒÙ„Ù‡ (Ù„Ø§Ø²Ù… per-widget).

---

> **Status:** ðŸŸ¢ READY AFTER AUDIT â€” ÙƒÙ„ Ø§Ù„Ù€ bugs Ø£Ø¯Ù†Ø§Ù‡ ØªØ­ÙˆÙ„Øª Ø¥Ù„Ù‰ tasks Ø¥Ù„Ø²Ø§Ù…ÙŠØ© Ø¯Ø§Ø®Ù„ Ù†ÙØ³ Ø§Ù„Ù…Ø±Ø­Ù„Ø© ÙˆÙ„Ø§ ØªÙØ¹ØªØ¨Ø± ØªØ¹Ø§Ø±Ø¶Ù‹Ø§ Ù…ÙØªÙˆØ­Ù‹Ø§.
**Executor Role:** Senior Frontend + Product Engineer
> 

---

## ðŸ“‹ Ù…Ù„Ø®Øµ

| Ø§Ù„Ù…Ø±Ø­Ù„Ø© | Ø§Ù„Ø§Ø³Ù… | Bug Count |
| --- | --- | --- |
| **Phase 06** | Dashboard + Today View | 5 bugs |
| **Phase 07** | Settings + Notifications | 5 bugs |

---

## ðŸ  Phase 06 â€” Dashboard

### Bugs Ø§Ù„Ù…ÙƒØªØ´ÙØ©

1. âŒ widgets ØªØ³ØªØ¯Ø¹ÙŠ AI ØªÙ„Ù‚Ø§Ø¦ÙŠÙ‹Ø§ = quota ÙŠÙØ­Ø±Ù‚ Ø¹Ù†Ø¯ load
2. âŒ widget ÙˆØ§Ø­Ø¯ ÙŠÙØ´Ù„ = dashboard ÙƒÙ„Ù‡ ÙŠÙØ´Ù„
3. âŒ Ù…Ù„Ø§Ø­Ø¸Ø§Øª ÙƒØ§Ù…Ù„Ø© ØªÙØ¹Ø±Ø¶ ÙÙŠ dashboard = content leak
4. âŒ Ù„Ø§ server-side cache Ù„Ù„Ù€ aggregations
5. âŒ Ù„Ø§ empty/error states Ù„ÙƒÙ„ widget Ù…Ù†ÙØ±Ø¯

---

### Step 06.1 â€” safeWidget Helper

```tsx
// src/features/dashboard/lib/safe-widget.ts
export async function safeWidget<T>(
  name: string,
  fn: () => Promise<T>,
  fallback: T
): Promise<T> {
  try {
    return await fn()
  } catch (e) {
    console.error(`[widget_failed:${name}]`, e)
    return fallback
  }
}
```

---

### Step 06.2 â€” Today Bundle (No AI on Load)

```tsx
// src/features/dashboard/api/today.ts
import 'server-only'
import { unstable_cache } from 'next/cache'
import { safeWidget } from './safe-widget'

export const getTodayBundle = (userId: string) =>
  unstable_cache(
    async () => ({
      tasks: await safeWidget('tasks', () => getTodayTasks(userId), []),
      habits: await safeWidget('habits', () => getTodayHabits(userId), []),
      expenses: await safeWidget(
        'expenses',
        () => getMonthExpenseSummary(userId),
        { totalCents: 0, byCategory: [] }
      ),
      // âœ… Ø¹Ù†Ø§ÙˆÙŠÙ† ÙÙ‚Ø· â€” Ù„Ø§ Ù…Ø­ØªÙˆÙ‰ ÙƒØ§Ù…Ù„
      notes: await safeWidget(
        'notes',
        () => getRecentNoteTitlesOnly(userId, { limit: 5 }),
        []
      ),
      // âŒ Ù…Ù…Ù†ÙˆØ¹ ØªÙ…Ø§Ù…Ù‹Ø§: AI summary Ù‡Ù†Ø§
      // âŒ Ù…Ù…Ù†ÙˆØ¹: AI call ÙÙŠ render path
    }),
    ['dashboard', userId],
    { revalidate: 30, tags: [`dashboard:${userId}`] }
  )()
```

---

### Step 06.3 â€” Dashboard Page (Streaming)

```tsx
// src/app/(app)/today/page.tsx
import { Suspense } from 'react'
import { Metadata } from 'next'
import { requireUser } from '@/lib/auth/require-user'
import { TasksWidget, TasksSkeleton } from '@/features/tasks/ui/TasksWidget'
import { HabitsWidget, HabitsSkeleton } from '@/features/habits/ui/HabitsWidget'
import { ExpensesWidget, ExpensesSkeleton } from '@/features/expenses/ui/ExpensesWidget'
import { NotesWidget, NotesSkeleton } from '@/features/notes/ui/NotesWidget'

export const metadata: Metadata = {
  title: 'Ø§Ù„ÙŠÙˆÙ… â€” Zenith',
  description: 'Ù…Ù„Ø®Øµ ÙŠÙˆÙ…Ùƒ: Ù…Ù‡Ø§Ù…ÙƒØŒ Ø¹Ø§Ø¯Ø§ØªÙƒØŒ ÙˆÙ…ØµØ§Ø±ÙŠÙÙƒ',
}

export default async function TodayPage() {
  await requireUser() // 401 Ù„Ùˆ Ù…Ø´ logged in

  return (
    <main className="grid gap-4 md:grid-cols-2 lg:grid-cols-3 p-4" dir="rtl">
      <Suspense fallback={<TasksSkeleton />}>
        <TasksWidget />
      </Suspense>
      <Suspense fallback={<HabitsSkeleton />}>
        <HabitsWidget />
      </Suspense>
      <Suspense fallback={<ExpensesSkeleton />}>
        <ExpensesWidget />
      </Suspense>
      <Suspense fallback={<NotesSkeleton />}>
        <NotesWidget />
      </Suspense>
    </main>
  )
}
```

---

### Step 06.4 â€” Widget Base Component (Error + Empty + Loading)

```tsx
// src/components/ui/WidgetShell.tsx
interface WidgetShellProps {
  title: string
  count?: number
  href?: string
  isEmpty?: boolean
  emptyLabel?: string
  children: React.ReactNode
}

export function WidgetShell({
  title, count, href, isEmpty, emptyLabel = 'Ù„Ø§ ÙŠÙˆØ¬Ø¯ Ø´ÙŠØ¡ Ø§Ù„Ø¢Ù†', children
}: WidgetShellProps) {
  return (
    <div className="rounded-xl bg-[var(--bg-elevated)] p-4 shadow-[var(--shadow-card)]">
      <div className="flex items-center justify-between mb-3">
        <h2 className="text-sm font-semibold text-[var(--fg-secondary)]">{title}</h2>
        {count !== undefined && (
          <span className="text-xs text-[var(--fg-muted)] bg-white/5 rounded-full px-2 py-0.5">
            {count}
          </span>
        )}
        {href && <a href={href} className="text-xs text-[var(--brand)] hover:underline">Ø§Ù„ÙƒÙ„</a>}
      </div>
      {isEmpty ? (
        <p className="text-sm text-[var(--fg-muted)] text-center py-6">{emptyLabel}</p>
      ) : children}
    </div>
  )
}

export function WidgetSkeleton({ title }: { title: string }) {
  return (
    <div className="rounded-xl bg-[var(--bg-elevated)] p-4 animate-pulse">
      <div className="h-4 w-24 bg-white/10 rounded mb-3" />
      <div className="space-y-2">
        {[1,2,3].map(i => <div key={i} className="h-8 bg-white/5 rounded" />)}
      </div>
    </div>
  )
}
```

---

### Step 06.5 â€” Guard: Ù„Ø§ AI ÙÙŠ Dashboard Load Path

```tsx
// src/features/dashboard/lib/no-ai-guard.ts
export function assertDashboardHasNoAICall(componentName: string): void {
  // Ù‡Ø°Ø§ ÙŠÙØ³ØªØ®Ø¯Ù… ÙƒÙ€ lint rule Ø£Ùˆ runtime check ÙÙŠ dev
  if (process.env.NODE_ENV === 'development') {
    if (typeof globalThis !== 'undefined' && (globalThis as any).__AI_CALL_DEPTH__ > 0) {
      throw new Error(`[${componentName}] Dashboard must not consume AI quota on page load`)
    }
  }
}
```

---

### ðŸ§ª Tests â€” Phase 06

```tsx
describe('Phase 06 â€” Dashboard', () => {
  it('does not call runAIWithQuota on dashboard load')
  it('returns fallback [] when tasks widget throws')
  it('returns fallback [] when habits widget throws')
  it('exposes only note titles, not full content')
  it('caches dashboard aggregates per userId with 30s revalidate')
  it('shows skeleton loading state for each widget independently')
  it('shows empty state when no tasks today')
})
```

### âœ… Definition of Done â€” Phase 06

- [x]  Ù„Ø§ AI call ÙÙŠ render path
- [x]  ÙƒÙ„ widget Ù„Ù‡ skeleton + empty + error state
- [x]  aggregation Ù…ÙƒØ§Ø´ Ø¨Ù€ Promise.all Ù…Ø¹ safeWidget
- [x]  notes ØªÙØ¹Ø±Ø¶ Ø¹Ù†Ø§ÙˆÙŠÙ† ÙÙ‚Ø·
- [x]  cache 30 Ø«Ø§Ù†ÙŠØ© per user

---

## âš™ï¸ Phase 07 â€” Settings + Notifications

### Bugs Ø§Ù„Ù…ÙƒØªØ´ÙØ©

1. âŒ settings update ÙŠØ³ØªØ¨Ø¯Ù„ JSON ÙƒØ§Ù…Ù„Ù‹Ø§ = data loss
2. âŒ `allowVaultContext` Ù…Ù…ÙƒÙ† ÙŠØªÙØ¹Ù‘Ù„ Ù…Ù† client
3. âŒ notification create Ø¨Ø¯ÙˆÙ† rate limit
4. âŒ Ù„Ø§ confirmation Ù„Ù„Ù€ dangerous settings changes
5. âŒ timezone ØºÙŠØ± Ù…Ø­Ù‚Ù‚ ÙƒÙ€ IANA string

---

### Step 07.1 â€” Timezone Validator

```tsx
// src/lib/validation/timezone.ts
export function isValidIanaTimezone(tz: string): boolean {
  try {
    new Intl.DateTimeFormat('en-US', { timeZone: tz })
    return true
  } catch {
    return false
  }
}

export function assertTimezone(tz: string): void {
  if (!isValidIanaTimezone(tz)) {
    throw new ValidationError({ timezone: ['Invalid IANA timezone'] })
  }
}
```

---

### Step 07.2 â€” Deep Merge Ù„Ù€ Settings

```tsx
// src/lib/utils/deep-merge.ts
export function deepMerge<T extends Record<string, unknown>>(
  base: T,
  patch: Partial<T>
): T {
  const out: Record<string, unknown> = { ...base }
  for (const [k, v] of Object.entries(patch)) {
    if (v && typeof v === 'object' && !Array.isArray(v) && typeof out[k] === 'object') {
      out[k] = deepMerge(out[k] as Record<string, unknown>, v as Record<string, unknown>)
    } else {
      out[k] = v
    }
  }
  return out as T
}
```

---

### Step 07.3 â€” DB Trigger: Force allowVaultContext = false

```sql
-- supabase/migrations/0005_settings_guards.sql
BEGIN;

-- ÙŠØ¶Ù…Ù† Ø£Ù† allowVaultContext Ù„Ø§ ÙŠÙÙØ¹Ù‘Ù„ Ø­ØªÙ‰ Ù„Ùˆ Client Ø£Ø±Ø³Ù„Ù‡
CREATE OR REPLACE FUNCTION public.profiles_force_vault_off()
RETURNS TRIGGER LANGUAGE plpgsql AS $$
BEGIN
  IF NEW.settings IS NOT NULL THEN
    NEW.settings = jsonb_set(
      COALESCE(NEW.settings, '{}'::jsonb),
      '{ai,allowVaultContext}',
      'false'::jsonb,
      true  -- create if not exists
    );
  END IF;
  RETURN NEW;
END $$;

DROP TRIGGER IF EXISTS trg_profiles_force_vault_off ON public.profiles;
CREATE TRIGGER trg_profiles_force_vault_off
BEFORE INSERT OR UPDATE ON public.profiles
FOR EACH ROW EXECUTE FUNCTION public.profiles_force_vault_off();

COMMIT;
```

---

### Step 07.4 â€” Update Settings Action (Safe)

```tsx
// src/features/settings/actions/update-settings.ts
'use server'
import { z } from 'zod'
import { requireUser } from '@/lib/auth/require-user'
import { serviceSupabase } from '@/lib/supabase/service'
import { deepMerge } from '@/lib/utils/deep-merge'
import { assertTimezone } from '@/lib/validation/timezone'
import { audit } from '@/lib/audit'

const schema = z.object({
  notifications: z.object({
    morningBriefAt: z.string().regex(/^\d{2}:\d{2}$/).optional(),
    eveningReviewAt: z.string().regex(/^\d{2}:\d{2}$/).optional(),
    enable: z.record(z.boolean()).optional(),
  }).optional(),
  privacy: z.object({
    analyticsOptOut: z.boolean().optional(),
  }).optional(),
  ai: z.object({
    responseLanguage: z.enum(['ar', 'en']).optional(),
    // âŒ allowVaultContext Ù…Ø´ Ù…Ø³Ù…ÙˆØ­ Ù…Ù† Ù‡Ù†Ø§ â€” Ø§Ù„Ù€ DB trigger ÙŠÙ…Ù†Ø¹Ù‡
  }).optional(),
  timezone: z.string().optional(),
  locale: z.enum(['ar', 'en']).optional(),
})

export async function updateSettingsAction(input: unknown) {
  const parsed = schema.parse(input)

  if (parsed.timezone) assertTimezone(parsed.timezone)

  const user = await requireUser()

  // Ø¬ÙŠØ¨ Ø§Ù„Ù€ settings Ø§Ù„Ø­Ø§Ù„ÙŠØ©
  const { data: cur } = await serviceSupabase
    .from('profiles')
    .select('settings, timezone')
    .eq('id', user.id)
    .single()

  // Deep merge â€” Ù„Ø§ replace
  const merged = deepMerge(cur?.settings ?? {}, parsed)

  // Force: allowVaultContext Ø¯Ø§ÙŠÙ…Ù‹Ø§ false (Ø§Ù„Ù€ DB trigger Ø¨ÙŠØ¹Ù…Ù„ Ù†ÙØ³ Ø§Ù„Ø´ÙŠØ¡ ÙƒÙ€ safety net)
  if (merged.ai) {
    (merged.ai as any).allowVaultContext = false
  }

  await serviceSupabase.from('profiles').update({
    settings: merged,
    ...(parsed.timezone ? { timezone: parsed.timezone } : {}),
    ...(parsed.locale ? { locale: parsed.locale } : {}),
  }).eq('id', user.id)

  await audit({ actorId: user.id, action: 'settings.update', metadata: { keys: Object.keys(parsed) } })
}
```

---

### Step 07.5 â€” Notification Rate Limit

```sql
-- supabase/migrations/0006_notification_buckets.sql
BEGIN;

CREATE TABLE IF NOT EXISTS public.notification_buckets (
  user_id UUID NOT NULL,
  type TEXT NOT NULL,
  hour_bucket TIMESTAMPTZ NOT NULL,
  count INT NOT NULL DEFAULT 0,
  PRIMARY KEY (user_id, type, hour_bucket)
);

CREATE OR REPLACE FUNCTION public.increment_notification_bucket(
  p_user UUID, p_type TEXT, p_hour TIMESTAMPTZ
)
RETURNS VOID LANGUAGE plpgsql SECURITY DEFINER SET search_path = public, pg_temp AS $$
BEGIN
  INSERT INTO public.notification_buckets(user_id, type, hour_bucket, count)
  VALUES (p_user, p_type, p_hour, 1)
  ON CONFLICT (user_id, type, hour_bucket)
  DO UPDATE SET count = notification_buckets.count + 1;
END $$;

COMMIT;
```

```tsx
// src/lib/notifications/rate-limit.ts
import 'server-only'
import { serviceSupabase } from '@/lib/supabase/service'
import { AppError } from '@/lib/result'

export async function assertNotificationLimit(
  userId: string,
  type: string,
  perHour = 5
): Promise<void> {
  const hour = new Date()
  hour.setMinutes(0, 0, 0)

  const { data } = await serviceSupabase
    .from('notification_buckets')
    .select('count')
    .eq('user_id', userId)
    .eq('type', type)
    .eq('hour_bucket', hour.toISOString())
    .maybeSingle()

  if ((data?.count ?? 0) >= perHour) {
    throw new AppError('NOTIFICATION_RATE_LIMIT', `${type}: max${perHour}/hour`, 429)
  }

  await serviceSupabase.rpc('increment_notification_bucket', {
    p_user: userId,
    p_type: type,
    p_hour: hour.toISOString(),
  })
}
```

---

### Step 07.6 â€” Settings Page Structure

```tsx
// src/app/(app)/settings/page.tsx
import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Ø§Ù„Ø¥Ø¹Ø¯Ø§Ø¯Ø§Øª â€” Zenith',
}

export default function SettingsPage() {
  return (
    <div dir="rtl" className="max-w-2xl mx-auto p-6 space-y-8">
      <h1 className="text-2xl font-bold text-[var(--fg-primary)]">Ø§Ù„Ø¥Ø¹Ø¯Ø§Ø¯Ø§Øª</h1>

      {/* Profile */}
      <section aria-labelledby="profile-heading">
        <h2 id="profile-heading" className="text-lg font-semibold mb-4">Ø§Ù„Ù…Ù„Ù Ø§Ù„Ø´Ø®ØµÙŠ</h2>
        {/* ProfileForm */}
      </section>

      {/* Notifications */}
      <section aria-labelledby="notif-heading">
        <h2 id="notif-heading" className="text-lg font-semibold mb-4">Ø§Ù„Ø¥Ø´Ø¹Ø§Ø±Ø§Øª</h2>
        {/* NotificationsForm */}
      </section>

      {/* Privacy */}
      <section aria-labelledby="privacy-heading">
        <h2 id="privacy-heading" className="text-lg font-semibold mb-4">Ø§Ù„Ø®ØµÙˆØµÙŠØ©</h2>
        {/* PrivacyForm â€” dangerous actions require confirmation */}
      </section>

      {/* Danger Zone */}
      <section aria-labelledby="danger-heading" className="border border-[var(--danger)]/20 rounded-xl p-4">
        <h2 id="danger-heading" className="text-lg font-semibold text-[var(--danger)] mb-4">Ù…Ù†Ø·Ù‚Ø© Ø§Ù„Ø®Ø·Ø±</h2>
        {/* DeleteAccount â€” requires password reauth */}
      </section>
    </div>
  )
}
```

---

### ðŸ§ª Tests â€” Phase 07

```tsx
describe('Phase 07 â€” Settings', () => {
  it('rejects invalid IANA timezone')
  it('forces ai.allowVaultContext to false even when client sends true')
  it('DB trigger forces allowVaultContext false on update')
  it('deep merges settings without losing existing keys')
  it('throttles notifications to 5/hour/type')
  it('returns 429 on 6th notification of same type in 1 hour')
  it('audits settings update with changed keys')
})
```

### âœ… Definition of Done â€” Phase 07

- [x]  settings update = deep merge Ù„Ø§ replace
- [x]  `allowVaultContext` Ù…Ø³ØªØ­ÙŠÙ„ ØªÙØ¹ÙŠÙ„Ù‡ (app + DB trigger)
- [x]  notifications rate limited: 5/hour per type
- [x]  timezone validated ÙƒÙ€ IANA Ù‚Ø¨Ù„ save
- [x]  dangerous actions ØªØ·Ù„Ø¨ confirmation
- [x]  audit log Ù„ÙƒÙ„ settings change

---

## ðŸ“ Ù…Ù„ÙØ§Øª Ø§Ù„Ù…Ø´Ø±ÙˆØ¹

```
src/features/dashboard/lib/safe-widget.ts
src/features/dashboard/api/today.ts
src/app/(app)/today/page.tsx
src/components/ui/WidgetShell.tsx
src/lib/validation/timezone.ts
src/lib/utils/deep-merge.ts
src/lib/notifications/rate-limit.ts
src/features/settings/actions/update-settings.ts
src/app/(app)/settings/page.tsx
supabase/migrations/0005_settings_guards.sql
supabase/migrations/0006_notification_buckets.sql
```
