{
  "name": "@zenith/workspaces",
  "version": "0.0.1",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@zenith/shared": "workspace:*",
    "@zenith/db": "workspace:*",
    "@zenith/audit": "workspace:*",
    "@zenith/auth": "workspace:*"
  }
}
