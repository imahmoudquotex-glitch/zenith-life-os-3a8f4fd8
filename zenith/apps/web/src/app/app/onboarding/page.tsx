import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Create Workspace — Zenith',
  description: 'Set up your first Zenith workspace.',
}

export { default } from './_page'
