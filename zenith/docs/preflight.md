# Preflight Check Results

- Node: >= 20.11
- pnpm: >= 9.0
- Postgres: >= 15.4
- git: >= 2.40
- tsc: >= 5.4
- `corepack enable` is expected to be configured.
- `packageManager`, `.nvmrc`, and `.tool-versions` are set up.

✅ All prerequisites met.
