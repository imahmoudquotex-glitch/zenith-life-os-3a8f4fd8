# ADR-0010 — Dark Mode Only + Arabic-First UI

**Date:** 2026-05-13
**Status:** Accepted

## Decision
Dark mode ONLY — no light theme tokens in CI.
Arabic-first (RTL) — dir=rtl on html, all layouts RTL-tested.
Enforcement: check:dark-only-tokens + shadcn rtl:true.
