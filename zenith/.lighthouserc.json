{
  "ci": {
    "assert": {
      "assertions": {
        "categories:performance": ["warn", { "minScore": 0.85 }],
        "categories:pwa": ["error", { "minScore": 1.0 }],
        "categories:accessibility": ["warn", { "minScore": 0.9 }],
        "categories:best-practices": ["warn", { "minScore": 0.9 }],
        "categories:seo": ["warn", { "minScore": 0.9 }],
        "is-on-https": ["error", { "maxLength": 0 }],
        "service-worker": ["error", { "maxLength": 0 }],
        "installable-manifest": ["error", { "maxLength": 0 }],
        "splash-screen": ["warn", { "maxLength": 0 }],
        "themed-omnibox": ["warn", { "maxLength": 0 }],
        "content-width": ["warn", { "maxLength": 0 }],
        "viewport": ["error", { "maxLength": 0 }],
        "maskable-icon": ["warn", { "maxLength": 0 }],
        "offline-start-url": ["error", { "maxLength": 0 }]
      }
    }
  }
}
